import { studentData } from "../../../../../shared/const/studentInfo";

export const initials = `${studentData.surname[0]}${studentData.name[0]}`;
export const fullName = `${studentData.surname} ${studentData.name} ${studentData.lastname}`;
